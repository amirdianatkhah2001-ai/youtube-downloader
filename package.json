{
  "name": "youtube-downloader",
  "version": "1.0.0",
  "private": true,
  "description": "Clean-room YouTube downloader companion browser extension",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "node --check src/popup.js src/options.js src/lib/downloader.js"
  }
}
